hash는 정렬이 필요 없으며 빠른 검색을 원할 때 사용된다.
hash 테이블에 자료를 저장할 때는 key값을 해시 함수에 대입하여 버킷번호가 나오면 그 버킷의 빈 슬롯에 자료를 저장.

hash_map 사용케이스
- 많은 자료를 저장하고 검색 속도가 빨라야 할 때
- 빈번하게 자료를 삽입하거나 삭제하지 않을 때


hash_map <int, float> hash_a;

hash_a.begin();
hash_a.clear();
hash_a.empty();

다른 컨테이너와 동일.
